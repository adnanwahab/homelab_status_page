{
  "name": "red-king-6e80",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest",
    
    "build": "echo 'script = ' | cat - dist/assets/index-*.js > src/index.js"
  
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.2",
    "wrangler": "^3.60.3",
    "vitest": "2.0.5"
  }
}